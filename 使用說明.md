# 神奇故事樹｜教學網　使用說明

語文　115 學年度 第 1 學期　第 4 單元　第 13～16 週

## 這是什麼

把〈神奇故事樹〉整個單元的教材（三則伊索寓言：龜兔賽跑／螞蟻和蟋蟀／北風和太陽）
做成一個**純靜態網站**，雙擊 `index.html` 就能用，不必安裝、不必連網
（只有影片與 PDF 是本機檔案，一樣離線可用）。

## 兩種開啟方式

| 開法 | 版本 | 差別 |
|---|---|---|
| 雙擊本資料夾的 `index.html` | **教室版** | 點名區可以上傳學生照片 |
| 用線上網址開啟 | **公開版** | 照片上傳自動關閉，只能輸入姓名 |

學生姓名存在瀏覽器的 localStorage、照片存 IndexedDB，**只留在那台電腦，不會上傳到網路，也不在 GitHub 裡**。

## 七個分區

| 分區 | 內容 |
|---|---|
| 🙋 點點名 | 抽籤點名，一輪每人只點一次，名單可匯出匯入 |
| 🃏 圖詞輪播 | 9 張學習圖卡，詞與說明各自可單獨發音 |
| 🎬 教學影片 | 主影片 1 支＋主題短片 3 支，右邊清單可自由切換 |
| 📚 教材教學 | 開場（P01–P03）→ 三個重點（龜兔賽跑／螞蟻和蟋蟀／北風和太陽），每個重點教學頁後接評量 → 收尾（P37–P41） |
| 📝 評量區 | 15 題（12 題正式＋3 題形成性）集中做一次，做完顯示各重點正確率；另可開學習單、練習單、圖卡與完整簡報 PDF |
| 🧠 自學區 | 圖卡牆，點一下放大，可自動輪播 |
| 🔗 延伸學習 | 3 支外部教學影片（巧虎TV、小馬哥說故事、樂樂TV），點了開新分頁到 YouTube，**這一區需要網路** |

側邊還有 🖊️ 畫筆與 ⏱️ 三組獨立計時器。

## 教材內容

- 頁圖 **62 頁**（教材 PPTX、PDF、紙本都由這 62 張產生）
- 三個重點：龜兔賽跑（不驕傲、堅持）／螞蟻和蟋蟀（提早準備、互相幫忙）／北風和太陽（溫和比強硬有效）
- 正式評量 **12 題**＋形成性評量 **3 題**，共 15 題兩選一，正解序列 `BBAABAABAABB`（正式）＋`BBA`（形成性）
- 學習圖卡 **9 張**
- 語音 **77 支**（`zh-TW-HsiaoChenNeural`，語速 -25%）
- 影片 **4 支**：教學影片（字幕版）、龜兔賽跑短片、螞蟻和蟋蟀短片、北風和太陽短片
- 延伸學習外部影片 **3 支**（老師 2026-09-10 確認的片單，已用 oembed 驗證有效）

## 要更新內容時

**`語文_第一學期_第4單元_神奇故事樹\pages\` 才是教材本體**，其餘（PPTX、PDF、紙本、影片、本網站）全是衍生檔。

```
1. python 重生頁圖.py p27            # 只重生要改的那一張（若無此腳本，用 生成頁圖.py --batch 重跑）
2. 逐字校對 → 覆蓋 pages\
3. 重跑下游：建簡報.py →（有引用到才）重建紙本.py／建教學影片.py
4. python ..\內容稽核.py <單元> --full   # ③④⑦ 要逐張讀圖
5. 網站：python 產出網站資料.py（在教材資料夾跑）→ 工具腳本\建置素材_圖片壓縮.py →（改到題目或口播才要）建置語音_全部.py
6. 跳版本號 → git commit → git push
```

🛑 **改了 `教學網站\` 裡的任何東西，`index.html` 的 `?v=` 就要加 1**。
`資源版本` 與 `語音.版本` 都住在 `config.js` 裡，而 `config.js` 本身也是用 `?v=` 載入的——
只改它們而不跳 `index.html` 的 `?v=`，回訪者會拿到快取裡的舊 `config.js`，等於沒更新。

## 題目的唯一來源

題目、選項與正解的唯一來源是素材資料夾的 `題庫.py`。
網站的 `js/data.js`、語音腳本用的 `工具腳本\語音內容.py`（由 `產出網站資料.py` 同一次自動產生）
都要跟它一致，改完題庫後在**教材資料夾**重跑：

```
python 產出網站資料.py
```
